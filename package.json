{
  "scripts": {
    "setup": "docker-compose up",
    "rebuild": "(docker build --tag=watchdock:latest .) && (docker extension update watchdock:latest) && ( docker extension dev debug watchdock )"
  },
  "dependencies": {
    "@emotion/react": "^11.11.0",
    "@emotion/styled": "^11.11.0",
    "@mui/material": "^5.13.4",
    "@reduxjs/toolkit": "^1.9.5",
    "@types/react": "^18.2.8",
    "cd": "^0.3.3",
    "react-redux": "^8.1.1"
  },
  "devDependencies": {
    "openai": "^4.0.0"
  }
}
